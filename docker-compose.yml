version: '3.9'

services:
  app:
    build: .
    volumes:
      - ./pystartowo/static:/code/pystartowo/static
    networks:
      - db-network
      - frontend-network
    restart: always

  frontend:
    image: nginx
    volumes:
      - ./nginx:/etc/nginx
      - ./pystartowo/static:/static
    ports:
      - "80:80"
    networks:
      - frontend-network
    restart: always
    depends_on:
      - app

networks:
  db-network:
    name: db-network
    external: true
  frontend-network:
    name: frontend-network
    external: true